form(action="/pictures/upload" method="POST" enctype="multipart/form-data")
  | Select an image to upload:
  input(type="file" name="avatar" accept="image/*")
  input(name="name" value="Deleav")
  input(type="submit" value="Upload Image")